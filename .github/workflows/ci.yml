---
# =====================================================================
# 🚀 CI - Integração Contínua do QA Oráculo
# ---------------------------------------------------------------------
# Este workflow é executado automaticamente em todo push ou pull request.
# Ele garante a qualidade do código antes de ser fundido na branch main.
#
# O que ele faz:
# 1. Verifica se o código segue PEP8 usando o Black.
# 2. Roda o Ruff (lint) para detectar más práticas, imports e bugs comuns.
# 3. Executa todos os testes unitários com pytest.
# 4. Gera relatório de cobertura de testes.
# 5. Falha o pipeline se a cobertura for menor que 95%.
# =====================================================================

name: CI

# ---------------------------------------------------------------------
# 🔔 Gatilhos de execução
# Executa este workflow quando:
# - houver push na branch main
# - ou quando um Pull Request for aberto para main
# ---------------------------------------------------------------------
on:
  pull_request: {}
  push:
    branches: [main]

  workflow_dispatch:
    inputs:
      python_version:
        description: "Versão do Python a ser testada (opcional)"
        required: false
        default: "3.12"
# ---------------------------------------------------------------------
# 👷 Definição dos jobs (etapas)
# Cada job roda em um ambiente isolado (VM Linux no GitHub).
# ---------------------------------------------------------------------
jobs:
  test:
    runs-on: ubuntu-latest  # sistema operacional do runner

    # -----------------------------------------------------------------
    # Estratégia de matriz → roda o mesmo job em várias versões do Python
    # Isso garante compatibilidade futura do projeto.
    # -----------------------------------------------------------------
    strategy:
      matrix:
        python-version: ['3.11', '3.12', '3.13']

    steps:
      # ---------------------------------------------------------------
      # 1️⃣ Baixa o código do repositório para dentro do runner
      # ---------------------------------------------------------------
      - uses: actions/checkout@v4

      # ---------------------------------------------------------------
      # 2️⃣ Configura o Python na versão especificada na matriz
      # ---------------------------------------------------------------
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # ---------------------------------------------------------------
      # 3️⃣ Cache das dependências pip
      # Se requirements.txt não mudou, reaproveita pacotes já instalados
      # ---------------------------------------------------------------
      - name: Cache pip
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: pip-${{ runner.os }}-${{ hashFiles('**/requirements.txt') }}

      # ---------------------------------------------------------------
      # 4️⃣ Instala dependências do projeto e ferramentas de qualidade
      # ---------------------------------------------------------------
      - name: Install deps
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install black ruff pytest pytest-cov

      # ---------------------------------------------------------------
      # 5️⃣ Verifica formatação com Black
      # Se algum arquivo não estiver no padrão, o job falha
      # ---------------------------------------------------------------
      - name: Lint (Black check)
        run: black --check .

      # ---------------------------------------------------------------
      # 6️⃣ Analisa más práticas com Ruff (lint)
      # ---------------------------------------------------------------
      - name: Lint (Ruff)
        run: ruff check .

      # ---------------------------------------------------------------
      # 7️⃣ Executa testes e gera relatório de cobertura em XML
      # ---------------------------------------------------------------
      - name: Run tests + coverage (XML)
        run: |
          pytest \
            --cov=app \
            --cov=database \
            --cov=graph \
            --cov=pdf_generator \
            --cov=state_manager \
            --cov=utils \
            --cov-report=xml

      # ---------------------------------------------------------------
      # 8️⃣ Verifica se a cobertura de testes é >= 95%
      # Se não for, falha o workflow
      # ---------------------------------------------------------------
      - name: "Gate - verifica se cobertura é suficiente"
        run: |
          python - <<EOF
          import xml.etree.ElementTree as ET

          # Lê o arquivo coverage.xml gerado pelo pytest
          r = ET.parse('coverage.xml').getroot().attrib

          # Extrai o percentual de cobertura
          cov = float(r['line-rate']) * 100
          print(f'📊 Cobertura total: {cov:.2f}%')

          if cov < 95.0:
              print("❌ Cobertura abaixo do mínimo exigido (95%)")
              exit(1)
          else:
              print("✅ Cobertura mínima atingida!")
          EOF

#pytest.ini

[pytest]
filterwarnings =
    # Ignora o aviso específico sobre o banco de dados não fechado que vem do LangChain/SQLite.
    ignore:unclosed database:ResourceWarning
markers =
    slow: marca testes mais demorados (ex.: subprocessos ou integração real)
